/**
 *
 * @param {Array} arr - an array of numbers to find the highest number in
 * @returns {number} - the highest number in the array
 */
function highestNumber(arr) {}

module.exports = highestNumber;
